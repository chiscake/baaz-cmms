<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="BAAZ.CMMS.App.Pages.Dispatcher.MaintenanceSchedule.MaintenanceScheduleChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="using:BAAZ.CMMS.App.Pages.Dispatcher.MaintenanceSchedule"
             xmlns:chart="using:BAAZ.CMMS.App.Controls.MaintenanceScheduleChart"
             xmlns:sizers="using:CommunityToolkit.WinUI.Controls"
             mc:Ignorable="d">

    <Grid VerticalAlignment="Stretch">
        <ProgressRing IsActive="{Binding IsChartLoading, Mode=OneWay}"
                      Width="32"
                      Height="32"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      IsHitTestVisible="False"
                      Visibility="{Binding IsChartLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

        <Grid VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="TimelineColumn"
                              Width="*"
                              MinWidth="320" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition x:Name="PanelColumn"
                              Width="380"
                              MinWidth="320" />
        </Grid.ColumnDefinitions>

        <chart:MaintenanceScheduleTimelineControl Grid.Column="0"
                                                  DataContext="{Binding}"
                                                  VerticalAlignment="Stretch" />

        <sizers:GridSplitter x:Name="SplitPane"
                             Grid.Column="1"
                             Width="8"
                             ResizeBehavior="PreviousAndNext"
                             ResizeDirection="Columns" />

        <local:MaintenanceSchedulePanelList Grid.Column="2"
                                            DataContext="{Binding}"
                                            VerticalAlignment="Stretch" />
        </Grid>
    </Grid>
</UserControl>
