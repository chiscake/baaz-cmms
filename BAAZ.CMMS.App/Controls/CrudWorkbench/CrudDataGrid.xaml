<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="BAAZ.CMMS.App.Controls.CrudWorkbench.CrudDataGrid"
             x:Name="GridRoot"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="using:BAAZ.CMMS.App.Controls.CrudWorkbench"
             MinHeight="0"
             VerticalAlignment="Stretch"
             mc:Ignorable="d">

    <Grid MinHeight="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Заголовок: фиксирован по вертикали, горизонтальная прокрутка синхронизируется с телом -->
        <ScrollViewer x:Name="HeaderScroll"
                      Grid.Row="0"
                      HorizontalScrollBarVisibility="Hidden"
                      HorizontalScrollMode="Enabled"
                      VerticalScrollBarVisibility="Disabled"
                      VerticalScrollMode="Disabled"
                      ViewChanged="HeaderScroll_ViewChanged">
        </ScrollViewer>

        <ListView x:Name="RowsList"
                  Grid.Row="1"
                  IsHitTestVisible="{x:Bind IsNotLoading, Mode=OneWay}"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  SelectionMode="None"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollMode="Enabled"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollMode="Enabled"
                  ScrollViewer.IsDeferredScrollingEnabled="False">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="MinHeight" Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:CrudDataGridRow />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Grid Grid.RowSpan="2"
              Background="{ThemeResource LayerFillColorDefaultBrush}"
              Opacity="0.85"
              Visibility="{x:Bind IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressRing IsActive="{x:Bind IsLoading, Mode=OneWay}"
                          Width="32"
                          Height="32"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center" />
        </Grid>
    </Grid>
</UserControl>
